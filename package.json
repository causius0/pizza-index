{
  "name": "pizza-index",
  "version": "0.2.0",
  "description": "Margherita Pizza Index — Big Mac methodology on AVPN-certified pizzerias, country averages",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "tsc && node dist/index.js",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^5.6.0"
  }
}
